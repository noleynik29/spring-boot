databaseChangeLog:
  - changeSet:
      id: create-roles-table
      author: noleynik29
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: ENUM('role_user','role_admin')
                  constraints:
                    nullable: false
                    unique: true