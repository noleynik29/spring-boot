version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: book_store_mysql
    restart: always
    environment:
      MYSQL_DATABASE: book_store
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: user
      MYSQL_PASSWORD: pass
    ports:
      - "5433:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password

volumes:
  mysql_data:
